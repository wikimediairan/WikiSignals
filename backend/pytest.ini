[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    smoke: live tests against public Wikimedia APIs (network required)
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
